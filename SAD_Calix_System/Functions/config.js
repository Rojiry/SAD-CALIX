/*
    XAMPP setup:
    Keep the CALIX project folder inside C:\\xampp\\htdocs.
    The HTML pages are inside /HTML and the PHP API is inside /api,
    so this relative URL works even if you rename the main project folder.
*/
window.CALIX_API_URL = "../api";
